{% set name = "rat" %}
{% set version = "3.0.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/rat-{{ version }}.tar.gz
  sha256: 46176d60186a37f24621079830922be15f60b727859da2a9e6487515c899ee79

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - rat = rat.cli.rat_cli:main

requirements:
  host:
    - python >=3.6
    - setuptools >=61.0
    - pip
  run:
    - python >=3.6
    - make
    - gfortran
    - rasterio
    - xarray
    - geopandas
    - rioxarray
    - earthengine-api
    - pandas
    - numpy
    - netcdf4
    - dask
    - scipy
    - scikit-learn
    - ruamel_yaml
    - yaml
    - gdown

test:
  imports:
    - rat
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/UW-SASWE/RAT
  summary: Resevoir Monitoring using Satellite Remote Sensing
  license: GPL-3.0-or-later
  license_file: LICENSE
  doc_url: https://rat-satellitedams.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - pritamd47
    - SanchitMinocha
